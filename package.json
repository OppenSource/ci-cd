{
  "name": "ci-cd",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "config": "jest.config.js",
    "testEnvironment": "jsdom",
    "jest": {
      "testMatch": [
        "**/tests/**/*.js"
      ],
      "testPathIgnorePatterns": [
        "node_modules"
      ]
    }
  },
  "devDependencies": {
    "jest": "^26.6.3"
  }
}
